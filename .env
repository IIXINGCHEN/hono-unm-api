# 服务配置
NODE_ENV=development # production, development, test
PORT=5678
HOST=0.0.0.0

# 日志级别 (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# CORS 配置
# ALLOWED_ORIGINS=http://localhost:3000,https://yourfrontend.com (多个用逗号分隔)
# 或者使用 '*' 允许所有 (生产环境不推荐直接使用 '*')
ALLOWED_ORIGINS=http://localhost:3000

# API 代理 (可选)
PROXY_URL=https://vcproxy.091017.xyz/

# 外部音乐 API
EXTERNAL_MUSIC_API_URL=https://music-api.gdstudio.xyz/api.php

# UnblockNeteaseMusic 服务特定配置 (可以通过config模块传递给服务)
ENABLE_FLAC=true

# JWT 密钥 (示例 - 务必使用强密钥并保密)
# JWT_SECRET=your-very-strong-and-secret-key
# JWT_EXPIRES_IN=1h

# 应用版本 (通常从 package.json 读取)
APP_VERSION=

# 服务名称 (用于日志)
APP_NAME=hono-unm-api
